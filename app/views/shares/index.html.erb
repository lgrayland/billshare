<div class="module"> 
  <h2>Share of the bill</h2>   
  <% @shares.each do |share| %>
    <%= share.grouping.user.first_name %> : <%= share.percent  %>&#37; 
    <%= link_to 'edit', edit_group_bill_type_share_path(@group, @bill_type, share.id), class: "button" %> <br>
  <% end %>
</div>
