<div class="bill_create">
  <h2>Create Bill</h2>
  <%= form_for [@group, @bill] do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit 'Create Bill', id: 'bill_create_button' %>
  <% end %>
</div>