<div>
  <%= form_for(@group) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit 'Edit group' %>
  <% end %>
</div>